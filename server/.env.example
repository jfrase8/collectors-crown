PORT=3001
# Comma-separated list of allowed CORS origins (Vercel URL in production)
CLIENT_ORIGIN=http://localhost:5173
# PostgreSQL connection string (Render PostgreSQL in production)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/collectors_crown
# Redis connection string (Render Key Value in production)
REDIS_URL=redis://localhost:6379
